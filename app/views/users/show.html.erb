<%= link_to 'Edit my Profile', edit_user_path(current_user.id) %>

<h1>My PlusOnes</h1>



<h2>Reviews</h2>

<p>Reviews for <%= @user.username %></p>

<% @user.user_reviews.each do |review| %>
  <p>
    <%= review.comment %>
    <br>
    <% if review.user != nil %>
      <em> by <%= review.user.email %></em>
      Added on: <%= review.created_at %>
    <% end %>
  </p>
<% end %>
